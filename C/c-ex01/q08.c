// segment a

// scanf ("%d %d", &x, &y);
// if (!(x <= 0 && y >= x))
//      printf ("A");
// else
//      printf ("B")

// segment b

// scanf ("%d %d", &x, &y);
// if (x > 0 || y < x)
//      printf ("A");
// else
//      printf ("B");

// Yes, both code segments perform the same task.