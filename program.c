#include <stdio.h>
#include <stdlib.h>
#include "7_BST.h"
#include "8_TST.h"

int Task1(ThreeNode* root) {
    if (root = NULL) return 0;

}

int Task2(ThreeNode* root) {
    if (root = NULL) return 0;
    
}

int Task3(ThreeNode* root) {
    if (root = NULL) return 0;
    
}

int Task4(ThreeNode* root) {
    if (root = NULL) return 0;
    
}

int Task5(ThreeNode* root) {
    if (root = NULL) return 0;
    
}

int Task6(ThreeNode* root) {
    if (root = NULL) return 0;
    
}

int Task7(ThreeNode* root) {
    if (root = NULL) return 0;
    
}

int Task8(ThreeNode* root) {
    if (root = NULL) return 0;
    
}